# Mini Assignment: im2col + CNN Filtering (Student Pack)

## What to do
1. Implement `im2col` in `code/im2col.py`.
2. Implement `conv2d_im2col` in `code/filters.py`.
3. Run `python code/main.py --image data/sample.png --save_dir results` to generate outputs.
4. Save the PNGs and commit your code.

## Install
```bash
pip install numpy pillow matplotlib
```
